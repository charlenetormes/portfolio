export enum DIRECTION {
    UP = 1,
    DOWN = 2,
    LEFT = 3,
    RIGHT = 4,
}

export enum PATHS {
    HOME = "/",
    ABOUT = "/about",
    PROJECTS = "/projects",
    CONTACTS = "/contacts",
}

export enum TECH {
    REACT = "react",
    VUE = "vue",
}
